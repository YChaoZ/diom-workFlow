package com.diom.gateway.config;

import org.springframework.context.annotation.Configuration;

/**
 * Gateway 配置
 * 所有路由配置已移至 application.yml，避免重复配置导致CORS问题
 *
 * @author diom
 */
@Configuration
public class GatewayConfig {
    // 路由配置已移至 application.yml
}

